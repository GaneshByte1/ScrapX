# Example environment variables for local development
NODE_ENV=development
# Enable verbose logging for an app/package, e.g. DEBUG=web:* or DEBUG=*
DEBUG=
# Example API URL used by apps/services
API_URL=http://localhost:3000
